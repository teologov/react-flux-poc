# react-flux-poc
React &amp; Flux App proof of concept

# How to install
* Install `nvm` https://github.com/creationix/nvm 
  * Run `nvm install iojs`
  * `nvm use 2`
* `npm install`
* `npm run dev`
* Open `localhost:4000`
